---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<p class="footer-title">Chez Lorraine</p>
		<p class="footer-tagline">Les recettes de grand-mère</p>
		<div class="footer-divider">
			<span>✦</span>
		</div>
		<p class="footer-quote">
			« Ces recettes ont été transmises avec amour de génération en génération. »
		</p>
		<p class="footer-copyright">
			&copy; {today.getFullYear()}
		</p>
	</div>
</footer>

<style>
	footer {
		background: white;
		border-top: 1px solid var(--border);
		padding: 3rem 1rem;
		margin-top: 2rem;
	}

	.footer-content {
		max-width: 500px;
		margin: 0 auto;
		text-align: center;
	}

	.footer-title {
		font-family: 'Playfair Display', Georgia, serif;
		font-size: 1.4rem;
		font-weight: 600;
		color: var(--text);
		margin: 0 0 0.25rem;
	}

	.footer-tagline {
		color: var(--text-muted);
		font-size: 0.95rem;
		margin: 0 0 1rem;
	}

	.footer-divider {
		color: var(--accent);
		font-size: 0.9rem;
		margin: 1rem 0;
	}

	.footer-quote {
		font-style: italic;
		color: var(--text-muted);
		font-size: 0.95rem;
		margin: 0 0 1.5rem;
		line-height: 1.6;
	}

	.footer-copyright {
		color: var(--text-muted);
		font-size: 0.85rem;
		margin: 0;
		opacity: 0.7;
	}
</style>
